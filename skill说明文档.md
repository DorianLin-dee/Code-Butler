# Code Helper 全能编程助手 Skill 说明

## 1、Skill 简介

这是一个全方位的编程助手 Skill，解决了开发过程中代码修改前后的备份恢复、多语言版本同步不一致、以及需要反复确认执行等问题，实现了自动增量备份、智能上下文理解、多语言同步验证等功能，适合需要多平台开发的程序员使用。

## 2、使用场景

### 为什么想做它？

在使用 AI 辅助开发的过程中，我遇到了几个痛点：
- 修改代码后如果不满意，需要手动撤销或 git 回退
- 让 AI 应用相同样式到所有页面时，经常遗漏一些文件或样式不一致
- 多语言版本（iOS/Android/小程序/Web）需要反复提醒 AI 同步，经常忘记
- 运行后才发现错误，然后复制错误给 AI，反复循环

### 之前遇到的麻烦

- 让 AI 修改代码后，无法方便地回退到之前的版本
- 全局应用样式时，总有遗漏的页面或样式不一致的地方
- 修改了一个平台的代码，忘记同步到其他平台
- 每次都要等运行后发现错误，再告诉 AI 修复

### 做出来之后能省掉哪些动作

- 自动在修改前备份，使用 `b1-b5` 快捷命令即可恢复
- 自动扫描所有页面，确保样式应用完整且一致
- 修改代码后立即验证，不等到运行后才报错
- 自动识别和同步所有语言版本
- 无需反复确认，白名单命令直接执行
- 通过暗号系统快速准确地表达需求

## 3、创作过程

### 需求分析与规划

首先，我梳理了自己的核心需求：
1. 备份和恢复功能 - 需要修改前后有备份，能快速回退
2. 上下文记忆 - 记住用户之前定义的样式和规范
3. 多语言同步 - 自动同步 iOS/Android/小程序/Web
4. 命令白名单 - 常用命令无需确认直接执行
5. 暗号系统 - 用简短暗号表达复杂需求

### 实现过程

#### 第一步：基础备份机制

```
需求：修改文件前自动备份，保留5步，支持 b1-b5 快捷恢复

实现：
- 定义了增量备份机制，只备份改动部分
- backup/1 为最新，backup/5 为最旧
- b1-b5 综合 N 步备份恢复，bi 恢复到初始状态
```

#### 第二步：上下文记忆与一致性检查

```
需求：记住用户的样式要求，确保全局应用一致

实现：
- 设计规范记录：回顾对话，建立项目规范清单
- 全页面扫描：文件发现 → 页面映射 → 全面检查
- 一致性验证：样式检测、代码模式检测、差异报告
```

#### 第三步：暗号系统

```
需求：用简短暗号准确表达需求，防止 AI 理解偏差

实现：
- c:/cc: 区分常量（不变量）和变量
- g:/l:/p: 控制范围（全局/本地/指定平台）
- v:/vv/check: 验证确认
- !/p1:/p2: 优先级控制
- dry:/force:/test: 执行模式
- 等等...
```

#### 第四步：代码验证与多语言同步

```
需求：代码写完后立即验证，自动同步多语言版本

实现：
- 前置验证：语法检查、导入检查、类型检查、API调用检查
- 平台特定检查：iOS/Xcode、小程序、Android、Web
- 自动同步：修改一个版本，自动同步到其他平台
```

### 核心配置文件

查看 [SKILL.md](file:///Users/dorian/Documents/solo/codeskill/.trae/skills/code-helper/SKILL.md) 了解完整配置。

## 4、使用步骤

### 安装与启用

1. 在项目根目录创建 `.trae/skills/code-helper/` 目录
2. 放入 `SKILL.md` 配置文件
3. 在对话中告诉 AI "使用 code-helper skill"

### 常用操作

#### 基础功能

| 操作 | 命令 | 说明 |
|------|------|------|
| 退回上一步 | `b1` | 恢复到1步前 |
| 退回3步前 | `b3` | 综合3步备份恢复 |
| 退回初始状态 | `bi` | 恢复到对话开始前 |

#### 暗号系统

```
g：样式 - 全局应用到所有页面
l：样式 - 仅当前文件
p：ios - 指定平台
v：检查样式 - 立即验证
vv：确保所有页面 - 双重验证
! 紧急修复 - 优先处理
dry：演示会改什么 - 预演不执行
note：这是临时的 - 重要备注
```

## 5、效果展示

### 使用前（传统方式）

```
用户：修改这个页面的样式
AI：（修改后运行报错）
用户：错误是XXX，修复一下
AI：（修复）
用户：应用到所有页面
AI：（应用到部分页面，漏了一些）
用户：不对，还有XX和YY页面没改
AI：（补充修改）
用户：还有颜色不对
AI：（修改颜色）
... 反复多次
```

### 使用后（有了 code-helper）

```
用户：g：修改这个页面的样式到所有页面
AI：（自动全页面扫描 → 修改前备份 → 应用到所有页面 → 验证）
AI：修改完成，已应用到 8 个页面，备份已创建（b1 可恢复）
用户：v：检查样式一致性
AI：验证通过，所有页面样式一致
```

### 多语言同步对比

使用前：
- 开发 iOS → 手动提醒同步到 Android → 再提醒同步到小程序
- 经常忘记，版本不一致

使用后：
- 开发 iOS → 自动同步到 Android、小程序、Web → 统一验证
- 无需手动提醒

## 6、Skill 链接

- Skill 配置文件：[.trae/skills/code-helper/SKILL.md](file:///Users/dorian/Documents/solo/codeskill/.trae/skills/code-helper/SKILL.md)
- 项目说明文档：[skill说明文档.md](file:///Users/dorian/Documents/solo/codeskill/skill说明文档.md)

## 7、总结与思考

### 收获

- **效率提升**：以前修改全局样式可能需要 5-6 次来回，现在一次完成
- **错误减少**：前置验证避免了很多运行时错误
- **沟通更高效**：暗号系统让需求表达更准确，减少误解

### 对 AI 工作方式的新感悟

通过这个 Skill，我意识到 AI 辅助开发的关键在于：
1. 明确的指令系统 - 让 AI 准确理解用户意图
2. 自动化的验证流程 - 把错误拦截在早期
3. 规范的工作流 - 形成可重复的高效模式

### 这个 Skill 目前最满意的地方

- 暗号系统 `c：/cc：` 完美解决了"一样但不一样"的场景（样式一样但保留不同颜色）
- `dry：` 预演模式让用户看到会改什么，心里更踏实
- 无需确认的白名单机制，大大提高了交互效率

### 后续还想怎么优化

- 添加 `learn：` 暗号，让 AI 学习用户的代码风格
- 支持自定义备份路径和数量
- 添加 `config：` 暗号，让用户在对话中配置 Skill 参数
- 集成更高级的代码检查工具

### 希望别人怎么体验或给我什么建议

建议按这个顺序体验：
1. 先试试 `g：` 和 `l：` 范围控制，感受一下全页面扫描
2. 再试试 `c：` 和 `cc：` 常量暗号，体验一下"一样但不一样"的场景
3. 最后用 `dry：` 看看预演模式如何工作

有任何问题或想法欢迎告诉我！
