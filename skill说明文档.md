# Code-Butler Skill 说明

## 1、Skill 简介

这是一个帮助多平台开发者解决代码修改备份恢复、多语言版本同步不一致、以及需要反复确认执行等问题的 Skill，实现了自动增量备份、智能上下文理解、暗号系统精准表达、约束条件协调、多语言同步验证等功能，适合需要多平台开发（iOS/Android/小程序/Web）的程序员和使用 AI 辅助开发的开发者使用。

## 2、使用场景

### 你为什么想做它？

在日常使用 SOLO（Trae Solo）进行编程的过程中，我发现了几个非常影响效率的痛点：每次告诉 AI 一个修改需求，结果改完发现不满意却不知道怎么快速回退；让 AI 把一个样式应用到所有页面，结果漏了好几个文件，改了又漏，漏了又改；同时开发 iOS 和小程序版本，改了一个版本总是忘记同步到另一个；有时候写了代码直接跑，报错信息复制给 AI，反复循环……

这些问题让我意识到：如果 AI 能有一套系统化的"工作规范"，而不是每次都从零开始理解我的需求，效率肯定会大幅提升。这就是 Code-Butler 诞生的原因——让 AI 像一个有经验的助手一样，自动备份、自动扫描、自动验证、自动同步。

### 你之前遇到了什么麻烦？

**痛点1：修改代码后无法快速回退**
```
我：把这几行代码换成另一种写法
AI：（完成了修改）
我：哎这个方案不对，还是改回去吧……
AI：之前的代码是什么来着？我忘了
```
结果就是每次修改都要手动复制备份，或者依赖 git 回退，但 git 的粒度太粗了（按提交来），无法精确回退到"前一步修改"。

**痛点2：全局应用样式总是有遗漏**
```
我：把这个按钮样式应用到所有页面
AI：（修改了3个页面）
我：还有2个页面没改
AI：（补上）
我：还有1个漏掉了
AI：（再补）
我：颜色又不一样了
AI：（再调整）
```
这种来回 5-6 次的情况每天都在发生，效率极低。

**痛点3：多语言版本不同步**
```
我：在 iOS 版本加个新功能
AI：（完成了）
我：顺便同步到小程序
AI：（又花了一轮对话）
我：再同步到 Android
AI：（再来一轮）
```
每次修改都要手动提醒 AI 同步到其他平台，非常繁琐。

**痛点4：先运行后报错再修复的循环**
```
我：写好这段代码
AI：（写好了）
我：（运行后）报错了，错误信息是XXX
AI：（修复）
我：（运行后）又报错，错误是YYY
AI：（再修复）
```
如果 AI 能在写代码时就做基本验证，能省掉多少来回？

**痛点5：两个约束条件总是无法同时满足**
```
我：页面加载时间要小于1秒，同时要显示完整数据列表
AI：（写了一版）
我：加载超时了
AI：（精简数据）
我：现在数据不全了
AI：（增加数据）
我：又超时了
……
```
AI 不知道如何主动分析冲突并提供方案，只能在两个极端之间反复尝试。

### 做出来之后能省掉哪些动作？

使用 Code-Butler 后，以下动作被自动化或大幅简化：

| 操作 | 之前需要做的 | 现在需要做的 | 节省的动作 |
|------|-------------|-------------|-----------|
| 代码备份 | 手动复制/git 提交 | 自动备份，无需任何操作 | 手动备份的所有步骤 |
| 回退修改 | 找历史记录/手动改回 | 说一句 `b1` 或 `b3` | 查找和手动恢复的时间 |
| 全局样式 | 反复告诉 AI 遗漏的文件 | 说一句 `g：样式` | 5-6 轮来回对话 |
| 多语言同步 | 每改一版提醒一次同步 | 自动同步到所有版本 | 每平台一轮提醒的对话 |
| 代码验证 | 运行→报错→复制→修复循环 | 写完自动验证 | 完整的"试错→修复"循环 |
| 约束冲突 | 在两个方案间反复尝试 | 自动提供多个方案供选择 | 反复尝试的时间 |

**保守估算**：每天开发过程中，这些来回对话至少占用 30% 的时间。使用 Code-Butler 后，相当于每天节省了约 1.5 小时的无效沟通时间。

## 3、创作过程

Code-Butler 完全是使用 Trae Solo 创建和迭代出来的。整个过程就是我与 AI 的一段段对话，每一次发现一个新问题，就添加一个新功能到 Skill 里。下面是完整的迭代路径：

### 阶段1：基础功能——备份机制

**触发场景**：我让 AI 修改了一段代码，结果改得不满意，想快速回退。发现 AI 本身没有"撤销"功能，只能让它重新写回去，但它可能已经忘了原来的代码是什么。

**关键提示词**：
```
我希望以后你生成修改前先备份，以便我退回上一步，备份3步，文件夹名backup，子文件夹名1-3。每多一步则删除一步，并将最新备份放在1，次之在2，再次之在3。
```

**工作流**：
```
用户要求修改 → 自动创建 backup/1 备份 → 执行修改
下次修改 → backup/1 → backup/2, 新备份放在 backup/1
用户说"b1" → 从 backup/1 恢复
```

后来我又把备份步数从 3 步扩展到了 5 步，保留更长的历史记录。

### 阶段2：自动持续完成

**触发场景**：我给了 AI 一个 PRD（产品需求文档），里面列了很多开发步骤。但 AI 做完一个就停下来等我继续说"继续"，很被动。

**关键提示词**：
```
ok，我还遇到一个问题：prd已经规划了开发顺序，但每次都要我说继续完成，你可以帮我发提示词直到完成度100%吗？写到skill里
```

**工作流**：
```
用户给PRD → 提取任务清单 → 自动开始任务1
完成任务1 → 自动开始任务2（无需用户说"继续"）
遇到问题 → 停下来等待用户决策
完成所有任务 → 报告100%完成
```

### 阶段3：上下文记忆与一致性检查

**触发场景**：我在上一轮对话中告诉了 AI 我想要的 UI 样式，它在一个页面实现得不错。然后我说"应用到全局"，结果它在其他页面改得不一样，或者漏了页面。我需要反复提醒它。

**关键提示词**：
```
下一个问题：你经常忘记我在上文中对你的要求，比如：我告诉你了我要的UI样式，你成功在一个界面中实现了，之后我让你应用于全局，你经常遗漏或者UI样式并不一样，这样我就得反反复复的让你改，如何杜绝此类问题。
```

**工作流**（这是 Code-Butler 最核心的部分）：
```
用户要求全局修改
    ↓
回顾上文所有设计规范（颜色/字体/间距/命名）
    ↓
扫描所有相关文件（建立页面结构树）
    ↓
找到已成功实现的示例
    ↓
完全复制相同方式应用到所有页面
    ↓
验证一致性（对比样式/代码模式）
    ↓
生成检测报告
```

这一步解决了"全页面扫描不全"和"一致性检测不到位"两个问题。

### 阶段4：暗号系统

**触发场景**：我发现我经常需要用很长的自然语言来表达一些简单意思，比如"只改当前文件，不要改其他文件"、"先看看会改哪些文件再决定"。如果有一套简短的"暗号"，效率会高很多。

**关键提示词**：
```
ok，为了方便你理解上下文，我可以告诉你哪些是不变量，暗号"c："，比如："c：我希望用户端的颜色是蓝色，服务端的颜色是紫色"。之后我告诉你让服务端UI与用户端一样的时候，我指的是一样的样式但保持服务端的紫色。除非我告诉你"cc：让服务端UI与用户端一样"，cc是change the constant。
```

还有：
```
帮我想想添加什么暗号，可以让你更好实现我的要求
```

**最终暗号清单**：

| 暗号 | 含义 | 示例 |
|------|------|------|
| `c：` | Constant 常量 | `c：用户端颜色是蓝色` |
| `cc：` | Change Constant 改变常量 | `cc：让服务端与用户端一样` |
| `g：` | Global 全局应用 | `g：样式` |
| `l：` | Local 仅当前文件 | `l：样式` |
| `p：` | Platform 指定平台 | `p：ios` |
| `v：` | Verify 立即验证 | `v：检查一致性` |
| `vv` | Double Verify 双重验证 | `vv：确保所有页面` |
| `check：` | 全面检查 | `check：多语言同步` |
| `!` | 紧急/重要 | `!：优先修复这个` |
| `p1：`/`p2：` | 优先级 | `p1：必须今天完成` |
| `dry：` | 预演模式 | `dry：演示会改什么` |
| `force：` | 强制执行 | `force：忽略警告继续` |
| `test：` | 测试模式 | `test：先验证不保存` |
| `q：` | Question 我的问题 | `q：这个颜色合适吗？` |
| `note：` | 重要备注 | `note：这是临时的` |
| `done：` | 标记完成 | `done：此功能已完成` |
| `explain：` | 需要解释 | `explain：说明你的方案` |
| `sync：` | 触发同步 | `sync：同步所有平台` |
| `nosync：` | 禁止同步 | `nosync：仅当前平台` |
| `must：` | 硬性约束 | `must：必须有点击事件` |
| `should：` | 软性约束 | `should：按钮最好是红色` |
| `prefer：` | 偏好约束 | `prefer：界面简洁一些` |
| `tradeoff：` | 权衡分析 | `tradeoff：分析性能和功能平衡` |
| `conflict：` | 冲突报告 | `conflict：检查是否有约束冲突` |

**暗号系统的工作流**：
```
用户输入（可能包含暗号）
    ↓
识别所有暗号标记
    ↓
根据暗号类型执行对应逻辑
    ├─ c： → 记录为常量（后续不可改，除非 cc：）
    ├─ g： → 启动全页面扫描（而非仅当前文件）
    ├─ v： → 完成修改后立即验证
    ├─ dry： → 只输出"会改哪些文件"，不实际执行
    └─ must/should： → 识别约束类型，后续可做冲突分析
    ↓
完成操作并报告结果
```

### 阶段5：代码验证与多语言同步

**触发场景**：我写完代码后总是要等运行后才发现错误，然后复制错误信息给 AI，反复循环。另外，我改了 iOS 版本总是忘记同步到小程序和 Web 版本。

**关键提示词**：
```
下一个问题：我很多时候就是让你写完代码，在我告诉你的环境运行比如：微信开发者工具、Xcode，然后它报错，我复制给你哪报错，不断重复。有时候没有同步编译成我需要的语言，我需要时不时提醒你更新。如何杜绝这样的问题。
```

**工作流**：
```
代码生成完成
    ↓
静态检查（语法/导入/类型/API调用）
    ↓
平台特定检查
    ├─ iOS/Xcode → 检查 IBOutlets/Storyboard/Info.plist
    ├─ 小程序 → 检查 app.json/page.json/wxss
    ├─ Android → 检查 Manifest/资源文件
    └─ Web → 检查 HTML/CSS/JS
    ↓
自动同步到其他语言版本
    ↓
验证所有语言版本
    ↓
完成（而非等待用户运行后报错）
```

### 阶段6：约束条件协调机制

**触发场景**：我给了两个矛盾的要求（比如"页面加载时间小于1秒"和"显示完整数据列表"），AI 不知道如何主动分析冲突，只能在两个极端之间反复尝试。

**关键提示词**：
```
再解决一个问题，写一个程序，我给了两个约束条件，总是不同时满足，如何用 skill 解决？
```

**工作流**：
```
用户给出多个约束条件
    ↓
识别并分类约束
    ├─ must： → 硬性约束（必须满足）
    ├─ should： → 软性约束（尽量满足）
    └─ prefer： → 偏好约束（优先级低）
    ↓
检测是否存在冲突
    ↓
如果有冲突 → 生成多个解决方案
    ├─ 方案A：优先满足高优先级，调整低优先级
    ├─ 方案B：折中方案，所有约束部分满足
    └─ 方案C：建议用户放宽某些约束
    ↓
向用户展示所有方案及优缺点
    ↓
等待用户选择方案后执行
```

### 最终 Skill 配置文件结构

经过以上 6 个阶段的迭代，最终的 SKILL.md 配置文件包含以下模块：
- ✅ 增量备份机制（5步备份 + 快捷恢复命令）
- ✅ 上下文记忆与一致性检查（全页面扫描 + 一致性验证）
- ✅ 命令白名单（无需确认直接执行）
- ✅ 上下文暗号系统（20+ 个精准暗号）
- ✅ 代码验证与多语言同步（前置验证 + 自动同步）
- ✅ 约束条件协调机制（冲突检测 + 多方案生成）
- ✅ 自动持续完成（按 PRD 推进到100%）

## 4、使用步骤

### 步骤1：获取 Skill 配置文件

从 GitHub 下载 SKILL.md 配置文件：

```
访问：https://github.com/DorianLin-dee/Code-Butler
下载：.trae/skills/code-helper/SKILL.md
```

或者直接 clone 整个项目：
```bash
git clone https://github.com/DorianLin-dee/Code-Butler.git
```

### 步骤2：放置到项目目录

在您的项目根目录下创建以下结构：

```
您的项目/
└── .trae/
    └── skills/
        └── code-helper/
            └── SKILL.md   ← 放在这里
```

### 步骤3：在对话中调用

在 Trae Solo 的对话中告诉 AI 使用这个 Skill：

```
请使用 code-helper skill 帮我开发
```

或者直接开始使用暗号，AI 会自动识别。

### 步骤4：常用操作示例

**场景A：修改代码（自动备份）**
```
我：把这段代码换成 async/await 风格
AI：（自动创建备份 backup/1 → 执行修改 → 完成）
我：嗯……不太对，还是改回去吧
AI：（执行 b1 → 恢复到修改前的状态）
```

**场景B：全局应用样式**
```
我：g：应用这个按钮样式到所有页面
AI：（自动全页面扫描 → 找到已实现的示例 → 应用到所有页面 → 验证一致性）
AI：修改完成，已应用到 8 个页面，备份已创建（b1 可恢复）
```

**场景C：使用暗号定义常量**
```
我：c：我希望用户端的颜色是蓝色，服务端的颜色是紫色
AI：（记录为常量）
我：让服务端UI与用户端一样
AI：（复制样式/布局，但保持服务端紫色不变）
我：cc：让服务端UI与用户端一样
AI：（真正改变常量，服务端颜色也改为蓝色）
```

**场景D：先预演再决定**
```
我：dry：帮我重构这个组件
AI：以下是我会修改的文件和内容摘要……
我：好的，执行
AI：（开始修改）
```

**场景E：多语言版本同步**
```
我：在 iOS 版本添加登录功能
AI：（完成 iOS 版本 → 自动识别并同步到小程序和 Web 版本 → 验证所有版本）
```

**场景F：约束条件协调**
```
我：must：接口必须返回完整数据，should：响应时间小于1秒
AI：（检测到潜在冲突 → 提供方案A/B/C 供选择）
我：选方案A（懒加载）
AI：（开始实现）
```

### 步骤5：快捷命令速查

在任何时候需要恢复备份，只需说：
- `b1` — 退回上一步
- `b3` — 退回3步前
- `b5` — 退回5步前
- `bi` — 恢复到对话开始前的初始状态

## 5、效果展示

### 场景1：全局样式修改——使用前后对比

**使用前**：
```
对话1：我：把这个按钮样式应用到所有页面
对话2：AI：（修改了3个页面）
对话3：我：还有2个页面没改
对话4：AI：（补上）
对话5：我：还有1个漏掉了
对话6：AI：（再补）
对话7：我：颜色又不一样了
对话8：AI：（再调整）
对话9：我：这次差不多了
耗时：9轮对话 × 约2分钟 = 18分钟
```

**使用后（有 Code-Butler）**：
```
对话1：我：g：把这个按钮样式应用到所有页面
对话2：AI：（自动全页面扫描8个文件 → 找到已实现的示例 → 应用到所有页面 → 验证一致性）
      AI：修改完成，已应用到 8 个页面，备份已创建（b1 可恢复）
耗时：2轮对话 × 约2分钟 = 4分钟
```

**效率提升**：18分钟 → 4分钟，节省约 **78%** 的时间

---

### 场景2：多语言同步——使用前后对比

**使用前**：
```
对话1：我：在 iOS 版本添加登录功能
对话2：AI：（完成 iOS，用时约5分钟）
对话3：我：顺便同步到小程序
对话4：AI：（完成小程序，用时约5分钟）
对话5：我：再同步到 Web 版本
对话6：AI：（完成 Web，用时约5分钟）
耗时：6轮对话 + 15分钟开发 = 约20分钟
```

**使用后（有 Code-Butler）**：
```
对话1：我：在 iOS 版本添加登录功能，同步到小程序和 Web
对话2：AI：（完成 iOS → 自动同步到小程序和 Web → 验证所有版本）
      AI：3个平台均已完成，iOS/小程序/Web 功能一致
耗时：2轮对话 + 15分钟开发 = 约16分钟
```

**效率提升**：20分钟 → 16分钟，节省约 **20%** 的时间（主要节省了提醒/等待的对话成本）

---

### 场景3：约束条件协调——使用前后对比

**使用前**：
```
对话1：我：页面加载时间小于1秒，同时显示完整数据列表
对话2：AI：（写了一版，完整数据）
对话3：我：加载超时了
对话4：AI：（精简数据，加载时间达标）
对话5：我：现在数据不全了
对话6：AI：（增加数据）
对话7：我：又超时了
对话8：AI：（再调整）
对话9：我：还是不行，换方案吧……
对话10：AI：（尝试分页方案）
对话11：我：这个方案可以
耗时：11轮对话 × 约2分钟 = 22分钟
```

**使用后（有 Code-Butler）**：
```
对话1：我：must：显示完整数据，should：加载时间小于1秒，tradeoff：分析
对话2：AI：检测到约束冲突，提供以下方案：
       方案A：懒加载，首屏0.8秒，完整数据滚动加载
       方案B：分页显示，每页加载0.5秒，需要翻页
       方案C：骨架屏 + 异步加载，首屏0.6秒显示骨架，2秒显示完整数据
对话3：我：选方案C
对话4：AI：（实现方案C，验证通过）
耗时：4轮对话 × 约2分钟 = 8分钟
```

**效率提升**：22分钟 → 8分钟，节省约 **64%** 的时间

---

### 整体效果总结

| 指标 | 使用前 | 使用后 | 提升 |
|------|--------|--------|------|
| 全局样式修改 | 5-9 轮对话 | 1-2 轮对话 | **70-80%** |
| 多语言版本同步 | 每平台需单独提醒 | 自动同步 | **50-60%** |
| 约束条件协调 | 在两个极端反复尝试 | 主动提供方案选择 | **60-70%** |
| 代码错误修复 | 运行→报错→复制→修复循环 | 写完立即验证 | **40-50%** |
| 修改后回退 | 手动找历史/重新写 | `b1` 一键恢复 | **80-90%** |

**保守估算**：在日常开发中，Code-Butler 可以节省约 **30-40%** 的对话沟通时间，让开发过程更顺畅。

## 6、Skill 链接

### 主要仓库
**GitHub**：https://github.com/DorianLin-dee/Code-Butler

### 核心文件

| 文件 | 路径 | 说明 |
|------|------|------|
| **SKILL.md** | `.trae/skills/code-helper/SKILL.md` | Skill 核心配置文件（最重要） |
| **README.md** | `README.md` | 项目说明文档 |
| **skill说明文档.md** | `skill说明文档.md` | 本说明文档 |
| **.gitignore** | `.gitignore` | Git 忽略规则 |

### 直接获取

```bash
# 方式1：clone 整个项目
git clone https://github.com/DorianLin-dee/Code-Butler.git

# 方式2：只下载 SKILL.md（最常用）
# 访问 https://github.com/DorianLin-dee/Code-Butler/blob/main/.trae/skills/code-helper/SKILL.md
# 点击 Raw 按钮 → 保存为文件
```

### 安装到您的项目

```
您的项目根目录/
├── .trae/
│   └── skills/
│       └── code-helper/
│           └── SKILL.md   ← 把上面的文件放在这里
├── src/
├── package.json
└── ...其他项目文件
```

然后在 Trae Solo 对话中说：`请使用 code-helper skill`

## 7、总结与思考

### 我的收获

#### 1. Skill 不是"写一段代码"，而是"写一套工作规范"

在创建 Code-Butler 的过程中，我最大的感悟是：Skill 并不是让 AI 学会写某种特定的代码，而是让 AI 学会一套**系统化的工作方式**。

比如：
- 备份机制不是让 AI 学会写备份脚本，而是让它养成"修改前先备份"的工作习惯
- 全页面扫描不是让 AI 学会写爬虫，而是让它养成"修改全局前先扫描确认范围"的工作习惯
- 前置验证不是让 AI 学会写测试，而是让它养成"写完代码先检查再交付"的工作习惯

这让我重新理解了 AI 辅助开发的本质：**AI 最大的潜力不是替代你写代码，而是帮你建立一套高质量的开发工作流，让你自己也变得更好**。

#### 2. 暗号系统的意外价值

暗号系统最初只是为了减少打字量，但使用后发现它有一个更深层的价值：**减少歧义**。

自然语言的问题在于模糊——"应用到所有页面"可以被理解成 10 种不同的含义。但 `g：样式` 是一个精确的指令，AI 不会误解。

这让我想到：**人和 AI 之间最高效的沟通方式可能不是"让 AI 理解更复杂的自然语言"，而是"人学会一套简单精确的指令语言"**。就像程序员和编译器的关系——不是让编译器理解英语，而是让程序员学会写精确的代码。

#### 3. Skill 应该是持续迭代的，不是一次性的

Code-Butler 不是一次写完的。它是在我和 AI 的 6 个阶段对话中逐步迭代出来的：
1. 发现一个痛点 → 添加一个功能
2. 再发现一个痛点 → 再添加一个功能
3. 再发现一个痛点 → 再添加一个功能

我觉得这才是 Skill 真正的生命力——它会随着你的使用经验和遇到的新问题不断成长。**一个好的 Skill，应该是你个人开发方法论的沉淀**。

#### 4. 对 AI 工作方式的新感悟

使用 Code-Butler 前后，我对"如何与 AI 高效协作"有了新的理解：

**之前的方式**：
```
我（需求方） ←→ AI（执行方）
问题：AI 是被动的，没有自己的工作规范，
每次都要重新理解上下文，效率低
```

**有了 Skill 之后**：
```
我（定义工作规范）
    ↓
Skill（承载规范，让 AI 按规范工作）
    ↓
AI（按规范高效执行）
```

变成了一个三层结构：**人定义规则 → Skill 承载规则 → AI 执行规则**。这样 AI 就不是一个"每次从零开始的新手"，而是一个"有经验、懂规范、知道你习惯"的老搭档了。

### 这个 Skill 目前最满意的地方

1. **暗号系统 `c：/cc：`**

   这个功能看似简单，但非常实用。它解决了"一样但不一样"这个很难用自然语言表达的场景。我发现很多开发中的需求其实都是"样式一样，但某几个属性要保持不同"，`c：/cc：` 让这种区分变得非常清晰。

2. **`g：` 全局扫描机制**

   之前每次让 AI 改全局都要反复提醒遗漏的文件。现在一个 `g：` 就自动完成扫描和一致性验证，这是让我感觉"效率明显提升"的功能。

3. **增量备份 + 快捷恢复**

   `b1`/`b3`/`b5` 这种简单命令能立即恢复，感觉就像 AI 有了一个"撤销按钮"，非常安心。

### 后续还想怎么优化

1. **`learn：` 暗号**

   我想添加一个 `learn：` 机制，让 AI 在开发过程中主动学习我的代码风格。比如当我反复修改 AI 的代码到某个风格时，AI 可以自动记录"这是用户偏好的写法"，下次直接生成符合风格的代码。

2. **可视化的备份历史**

   目前备份是文本形式的 diff。如果能有一个简单的工具，显示 backup/1 到 backup/5 的修改摘要，让我像看 git log 一样快速回顾"这几步都改了什么"，恢复时会更有信心。

3. **项目级规范文件**

   目前上下文记忆是靠"回顾上文"实现的。我想改成在项目根目录放一个 `project-spec.md` 或 `.code-butler` 文件，里面写清楚项目的颜色规范、组件命名、技术选型等。这样即使开新对话，AI 也能读取这些规范，而不依赖对话上下文。

4. **更多平台特定检查规则**

   目前的前置验证比较基础。我希望针对不同框架（比如 Vue、React、微信小程序、SwiftUI）补充更详细的检查规则，让前置验证能发现更多真正会导致运行错误的问题。

### 希望别人怎么体验或给我什么建议

**推荐的体验顺序**：

1. **先试试 `g：` 和 `l：`**

   找一个你有多个页面的项目，让 AI 用 `g：` 全局修改某个样式。感受一下 AI 自动扫描所有页面、一次性改完、自动验证一致性的过程。对比一下之前需要反复提醒的体验。

2. **再试试 `c：` 和 `cc：`**

   故意定义一个"常量"（比如某个颜色），然后用"一样但不同颜色"的需求测试。看看 AI 能不能正确理解"复制样式但保持颜色不同"这种微妙的区分。

3. **最后试试 `dry：`**

   让 AI 用 `dry：` 预演一个重构。看看它能不能准确告诉你会改哪些文件，而不实际执行。这对于大的改动特别有帮助。

**欢迎给我提建议的方向**：

- 如果您在使用中发现某个暗号不好用、容易混淆，欢迎告诉我应该怎么改进
- 如果您遇到了 Code-Butler 没有覆盖的新痛点，欢迎提 Issue 或直接添加新功能
- 如果您对暗号系统有新的想法（比如有更简洁的表达方式），非常期待交流
- 如果您在多平台开发（特别是原生 iOS/Android）中有独特的经验，欢迎分享如何让 Code-Butler 更好地支持这些场景

您可以通过 GitHub Issue 或直接在对话中告诉我您的想法。这个 Skill 会持续迭代，希望它能帮助更多人提高和 AI 协作的效率！
